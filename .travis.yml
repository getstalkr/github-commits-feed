language: node_js
node_js:
  - '6'
  - '4'
script:
  - npm run code:build  # build
  - npm run code:test:spec  # run mocha unit tests with coverage
after_script:
  # - 'cat coverage/lcov.info | ./node_modules/.bin/coveralls' # sends the coverage report to coveralls
